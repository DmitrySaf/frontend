# Bean — мастер-план разработки веб-приложения

> **Назначение:** единый навигационный документ по всей работе. После одобрения копируется в `frontend/docs/plan.md` и обновляется после каждого этапа (чекбоксы, лог решений). Работаем строго по этапам: **вопросы по странице → мок-данные по схеме → реализация → проверка → коммит**.

## Контекст

Строим **Bean** — русскоязычную платформу, где авторы ведут платные сообщества (курсы, посты, чаты) и зарабатывают на подписках. Кодовая база — Next.js 15 (App Router) + Supabase + React Query + Tailwind v4, архитектура FSD (`app → pages → widgets → features → entities → shared`).

**Источники истины:**
- Дизайн-система: `bean-design-system/project/` (README = гайдлайны, `wireframes/*.jsx` = вайрфреймы недостающих экранов, `ui_kits/bean-app/` = готовые референсы экранов, `colors_and_type.css` = токены)
- UX-референсы: Discord (структура сообщества, чат) и Whop (монетизация, витрина)
- Контракт данных: `frontend/docs/db-schema.md` (утверждён; моки повторяют его форму)
- Копирайтинг: русский, sentence case, обращение на «вы», без эмодзи

## Глобальные правила (утверждены пользователем)

| Решение | Значение |
|---|---|
| Бренд | **Bean** (лаймовый боб), ProFound удалён |
| Данные | **Сначала UI на моках** по форме схемы; реальные миграции + подключение БД — предпоследний этап |
| Платежи | **Симуляция**: «оплата» мгновенна, транзакции пишутся в БД; реальный провайдер — после v1 |
| Тарифы | Полная гибкость: recurring с любым периодом/ценой/скидкой, one-time платёж за вход, бесплатно = без активных тарифов |
| Комментарии | Раскрывающаяся лента под постом, один уровень |
| Чат v1 | Отправка + редактирование/удаление своих + модерация админом. Реакции/reply — v2 |
| Видео курсов | Supabase Storage (bucket `lesson-videos`), HTML5-плеер, signed URLs |
| OAuth VK/Яндекс | Скрыт до реальной интеграции |
| OTP | 6 цифр (дефолт Supabase), стилизация по DS |
| Редирект после входа | Последнее сообщество (`localStorage bean:last-community`) → первое → пустое состояние с CTA |
| Rail-кнопки «сообщения»/«поиск» | Скрыты до v2 |
| Аватары без лого | Инициалы на детерминированном пастельном фоне (Discord-style), общий компонент `Avatar` |
| Модалка создания | Без загрузки лого (лого — в админке «Внешний вид») |
| Роли | Модель A: инлайн-переключатель «Просмотр ↔ Редактирование» для админа |
| Адаптив | Desktop-first без жёстких ширин; мобильный UI — после v1 |
| Git | Коммит после каждого завершённого и проверенного этапа (`feat: этап N — …`) |

## Статус этапов

- [x] **Этап 1 — Фундамент**: аудит, ребрендинг, схема БД
- [x] **Этап 2 — Авторизация**
- [x] **Этап 3 — Rail + создание сообщества**
- [x] **Этап 4 — Сайдбар сообщества: категории, табы, «Новый таб»**
- [ ] **Этап 5 — Чат** ← *текущий*
- [ ] **Этап 6 — Посты**
- [ ] **Этап 7 — Курсы**
- [ ] **Этап 8 — Админ-разделы (дашборд, внешний вид, настройки)**
- [ ] **Этап 9 — Публичная витрина + вступление**
- [ ] **Этап 10 — Настройки: верификация, вывод средств, транзакции**
- [ ] **Этап 11 — Роли (модель A) по всем поверхностям**
- [ ] **Этап 12 — База данных: миграции, RLS, подключение, realtime, Storage**
- [ ] **Этап 13 — Финальный e2e-прогон и полировка**

---

## ✅ Этап 1 — Фундамент (готово)

- Аудит кода и дизайн-системы; инвентаризация экранов
- Ребрендинг: wordmark + лого на логине ([Login.tsx](src/pages/login/ui/Login.tsx)), metadata в [layout.tsx](app/layout.tsx), префикс `bean.com/` в модалке создания
- `font-sans`/`font-mono` привязаны к Geist/Geist Mono в [globals.css](app/globals.css)
- Схема данных утверждена → `docs/db-schema.md` (18 таблиц)

## ✅ Этап 2 — Авторизация (готово)

- OTP-ячейки по DS (Geist Mono, «·», focus-ring 2px primary, danger-состояние) — [ConfirmationForm.tsx](src/features/auth/ui/ConfirmationForm.tsx)
- Кулдаун 60 с на повторную отправку; спиннер + «Отправляем код…» (доработан [button.tsx](src/shared/components/button.tsx): isLoading не заменяет текст)
- OAuth-кнопки удалены
- Редиректы: `/` по сессии; `/communities`-резолвер ([CommunityListPage](src/pages/community-list/ui/CommunityListPage.tsx)): последнее (пишется `CommunityVisitTracker` в layout сообщества) → первое → пустое состояние «Создайте своё сообщество» (CTA открывает модалку через `?create=community`)
- Попутно починено 5 ранее сломанных ошибок сборки (реэкспорты, `Omit`→`Exclude`, типы community API, prerender nuqs → `force-dynamic` на (authenticated)/layout)
- Проверено: build проходит, живой сервер отдаёт корректные редиректы и брендинг

**Осталось вне v1:** реальная интеграция VK ID / Яндекс ID.

## ✅ Этап 3 — Rail + создание сообщества (готово)

- **`Avatar`** ([Avatar.tsx](src/shared/components/Avatar.tsx)): Radix-примитив, `src?`/`name`/`size s|m|l`/`shape square|circle`, инициал на детерминированном пастельном фоне (8 цветов от хэша имени)
- **`Tooltip`** ([Tooltip.tsx](src/shared/components/Tooltip.tsx)): Radix, тёмная плашка по DS (12.5px, rounded-xl, стрелка)
- **Rail**: кнопки «сообщения»/«поиск» скрыты до v2; 🐼 → `Avatar` (square); активный индикатор 3×14px чёрная полоса справа; тултипы названий; лого кликабелен → `/communities`
- **ProfileButton**: реальный профиль (`useProfileQuery`), клик по имени/шестерёнке → `/settings/profile`, «···» → dropdown «Выйти из аккаунта» (danger)
- **Модалка создания**: helper «Уникальное имя — подставляется автоматически», после создания — редирект в новое сообщество
- Установлены `@radix-ui/react-avatar`, `@radix-ui/react-tooltip`

**Осталось вне v1:** мессенджер и глобальный поиск (кнопки rail вернуть при реализации).

## ✅ Этап 4 — Сайдбар сообщества (готово)

**Решения:** «Главной» нет — вход в сообщество открывает первый таб (Discord-style); шапка сообщества (⌘K/help/bell) убрана целиком; «Покинуть» = конферм-модалка (владельцу пункт не показывается); DnD порядка табов — v2.

- **Mock-слой**: [mockCollection.ts](src/shared/utils/mockCollection.ts) — localStorage-коллекции формы db-schema; entity [channel](src/entities/channel/index.ts) (категории+каналы, сид «Начало»+#общий-чат, slug-транслитерация с уникальностью, CRUD-мутации с тостами)
- **CommunitySidebar** переписан: баннер (обложка 132px + оверлей / плоский 60px, Avatar square), chevron-меню (админ: Настройки/Внешний вид/Дашборд + подпись «только для админа»; Пригласить; Покинуть danger), категории со сворачиванием и «+» по hover, строки каналов с иконкой типа (hash/newspaper/book-open) и active-состоянием по DS
- **Модалка «Новый таб»** ([channel-create-modal](src/widgets/channel-create-modal/ui/ChannelCreateModal.tsx)): 3 плитки типов, название с плейсхолдером по типу, селект категории + «Новая категория» инлайн; после создания — переход в таб
- **Инвайт**: диалог с копируемой ссылкой на публичную страницу
- **Роутинг**: `[slug]` → редирект в первый таб; `[tabSlug]` → тайтл-бар (имя+тип-пилл) + плейсхолдер по типу (заменяются на этапах 5–7); `[slug]/admin/{dashboard,appearance,settings}` — заглушки «в разработке» (этап 8)
- **Dropdown** расширен: note-подписи («только для админа»), danger-токен вместо red
- Удалены: `community-header`, `community-home`, старые статичные MAIN_TABS/SIDEBAR_SECTIONS

## Этап 5 — Чат

**Референсы:** `wire-community.jsx` (ScreenCommunityChat), Discord.

Задачи:
1. Entity `message` (mock-store, форма = `messages` из схемы; генератор истории сообщений)
2. Страница таба-чата: тайтл-бар канала (иконка + имя + тип-пилл), лента с разделителями по дням, группировка подряд идущих сообщений автора (Discord-style), метка «автор» у владельца
3. Композер: «Написать в #канал…», отправка Enter, кнопки plus/smile/send (plus/smile — заглушки v1?)
4. Редактирование/удаление своих сообщений (hover-меню «···»), удаление любых — админ
5. Скролл-поведение: прилипание к низу, подгрузка вверх (mock-пагинация)

**Вопросы перед этапом:** вложения-картинки в v1 или позже; typing-индикатор; максимальная длина сообщения.

## Этап 6 — Посты

**Референсы:** `wire-community.jsx` (ScreenCommunityPosts), `wire-roles-posts.jsx`, DS README «Posts».

Задачи:
1. Entity `post` переписать с моков-заглушек на mock-store формы схемы (`posts`, `post_likes`, `post_comments`, `post_bookmarks`)
2. Лента таба «посты»: композер (аватар + recessed-поле + кнопка изображения + «Опубликовать»), фильтр-пиллы
3. `PostCard`: автор-строка (аватар, имя, «· автор» в primary, отн. время, пин «Закреплено», «···»), заголовок text-lg/700, текст, обложка full-bleed, панель реакций (heart → danger при лайке, message-circle, share-2, bookmark); текстовый пост — без обложки
4. Комментарии: клик по счётчику раскрывает ленту под постом + поле ввода (один уровень)
5. Создание/редактирование поста: модалка или инлайн-расширение композера (спросить)
6. «···»-меню по ролям: свой пост — редактировать/удалить; админ — закрепить/открепить/удалить любой

**Вопросы перед этапом:** редактор поста (модалка vs страница vs инлайн); фильтры «Все/Объявления/Обсуждения» — это типы постов или теги; шаринг — копия ссылки?

## Этап 7 — Курсы

**Референсы:** `wire-community.jsx` (ScreenCommunityCourses), `wire-roles-courses.jsx` (конструктор, модель A).

Задачи:
1. Entities `course`/`module`/`lesson`/`lesson_progress` — mock-store по схеме (заменить текущие моки-заглушки course)
2. **Плеер (участник):** левая панель 256px — название курса, прогресс-бар N/M, модули (uppercase-заголовки) с уроками (check/play/lock, тайминг Geist Mono); правая — видео-плейсхолдер, пиллы «Урок N из M» + длительность, заголовок, описание, кнопки Назад/Следующий урок/Отметить пройденным
3. **Конструктор (админ, модель A):** переключатель «Просмотр ↔ Редактирование» в топ-баре курса; в режиме редактирования — добавление/переименование/удаление/перестановка модулей и уроков, загрузка видео (mock: превью локального файла), описание урока
4. Логика «locked» уроков: v1 — последовательное прохождение? (спросить)

**Вопросы перед этапом:** блокировка уроков (последовательно/все открыты/по тарифу); материалы к уроку (файлы) в v1; текстовые уроки без видео.

## Этап 8 — Админ-разделы

**Референсы:** `wire-admin.jsx` (AdminShell, Дашборд, Внешний вид, Настройки сообщества).

Задачи:
1. **AdminShell**: маршруты `/communities/[slug]/admin/{dashboard|appearance|settings}`; хедер с «назад», подписью «только для админа»; вход из chevron-меню сайдбара
2. **Дашборд**: stat-карточки (доход/участники/подписки, значение Geist Mono text-2xl, тренд), бар-чарт дохода по месяцам, лайн-чарт роста участников, таблица тарифов (название/цена/подписчики/доход), переключатель периода. ⚠️ Перед чартами — прочитать skill `dataviz`. Mock-данные согласованы с формой `transactions`/`subscriptions`
3. **Внешний вид**: загрузка обложки (1600×400) и лого (512×512) с локальным превью, выбор акцентного цвета (свотчи + свой), тема (светлая/тёмная/системная), live-превью публичной карточки сообщества справа
4. **Настройки сообщества**: название, описание, публичный адрес (prefix `bean.com/`), видимость (публичное/закрытое — карточки-радио), тарифы: список (drag-handle, имя+бейдж скидки, цена/период Geist Mono, тумблер is_active, редактировать/удалить) + модалка тарифа (kind recurring/one_time, цена, период, скидка) + «Добавить тариф»
5. Sticky save-bar «У вас есть несохранённые изменения» (переиспользовать из настроек профиля)

**Вопросы перед этапом:** валюта только ₽; удаление сообщества (danger-zone) здесь же; акцентный цвет — на что влияет в v1.

## Этап 9 — Публичная витрина + вступление

**Референсы:** `wire-create-preview.jsx` (ScreenCommunityPreview, StatePreviewAuthDialog, StatePreviewLoading), текущий WIP `src/pages/community-preview/`.

Задачи:
1. Достроить `/[slug]`: PublicHeader (Bean + «Войти»), медиа-карусель с тумбами, заголовок + пилл «Проверено» (если автор верифицирован), описание, блок «Что внутри», sticky-колонка: карточка тарифов (радио-строки, выбранный = 2px primary ring + fill, бейджи скидок; free = кнопка без тарифов; one-time = разовая цена) + карточка автора
2. Вступление: гость → auth-диалог (rounded-20, email-флоу как на логине); юзер → симуляция оплаты (мгновенно) → membership в mock-store → редирект в сообщество
3. Состояния: скелетон загрузки, 404 несуществующего slug, «уже участник» → кнопка «Открыть сообщество»
4. Медиа витрины: откуда берутся (админ «Внешний вид» или отдельная галерея?) — спросить

**Вопросы перед этапом:** экран симуляции оплаты (модалка-чек с фейковой картой?); закрытые сообщества — что видно по прямой ссылке; helper «Что внутри» — автогенерация из табов или ручной ввод.

## Этап 10 — Настройки: верификация, вывод средств, транзакции

**Референсы:** `wire-settings.jsx` (все 5 экранов + пустые состояния), `wire-app.jsx` секция settings.

Задачи:
1. **Верификация**: баннер 3 состояний (none: recess + CTA «Пройти верификацию»; pending: янтарный «до 2 рабочих дней»; passed: зелёный + значок в профиле) — семантические токены из DS (`--warning`/`--success` добавить в `@theme`); флоу подачи: выбор типа (паспорт/самозанятый/ИП/ООО) + форма (mock) → pending
2. **Вывод средств**: список карт (радио «Выбрана для вывода», номер Geist Mono, «···»-меню удаления), модалка «Добавить карту» (ФИО раздельно + номер), пустое состояние
3. **Транзакции**: фильтры Все/Поступления/Выводы, строки (иконка направления, название, дата, статус-пилл, ±сумма Geist Mono), пустое состояние; кнопка «Вывести средства» (симуляция: списание → транзакция «В обработке»)
4. **Профиль/Конфиденциальность**: сверить с вайрфреймом (sticky save-bar, EditRow «Изменить» для почты/телефона, тумблеры приватности) — уже реализованы, полировка

**Вопросы перед этапом:** смена почты — с подтверждением кодом; телефон в v1 (в БД поля нет — добавить в схему?); баланс — где показывать (шапка транзакций?).

## Этап 11 — Роли (модель A) по всем поверхностям

**Референсы:** `wire-roles-models.jsx` (матрица прав), `wire-roles-posts.jsx`, `wire-roles-courses.jsx`, `roles-app.jsx`.

Задачи:
1. Mock-слой `community_members` (роль owner/admin/member текущего пользователя в каждом сообществе) + hook `useCommunityRole(slug)`
2. Глобальный переключатель «Просмотр ↔ Редактирование» для админа (курсы-конструктор, посты-модерация, сайдбар «+»)
3. Скрытие админ-пунктов (chevron-меню, admin-маршруты — guard), «···»-меню по ролям, модерация чата
4. Матрица прав задокументировать в `docs/db-schema.md` (для будущих RLS-политик)

## Этап 12 — База данных: миграции и подключение

Задачи:
1. Миграции Supabase по `docs/db-schema.md` (все 15 новых таблиц + расширение communities) + RLS-политики по матрице прав + `pnpm gen-supabase-types`
2. Storage-бакеты: `community-covers`, `community-logos`, `post-covers`, `lesson-videos` (+политики)
3. Замена mock-store на реальные API послойно (entity за entity: members → channels → messages → posts → courses → tiers/subscriptions/transactions → verification/payout), сохраняя формы типов
4. Realtime: чат (insert/update/delete messages), посты, инвалидация кэша по skill `realtime`
5. Симуляция платежей: RPC/edge function `simulate_purchase` → subscription + transaction атомарно
6. Сиды для демо-данных

## Этап 13 — Финальный e2e-прогон и полировка

1. Сценарий автора: регистрация → создание сообщества → структура (чат/посты/курс) → контент → тарифы → внешний вид → верификация → карта → «продажа» → дашборд → вывод средств → транзакции
2. Сценарий участника: витрина → вступление (бесплатно и платно) → чат/посты/курс → прогресс → покинуть сообщество
3. Все пустые/загрузочные/ошибочные состояния; тосты по паттерну mutations; консистентность DS (радиусы, цвета, копирайтинг)
4. `pnpm build` + `pnpm check` чистые; финальный коммит

---

## Регламент работы по этапу

1. Перед этапом: перечитать соответствующий вайрфрейм + задать вопросы из секции этапа (AskUserQuestion)
2. Мок-данные строго по `docs/db-schema.md` (snake_case «в БД», camelCase после мапперов)
3. Компоненты: сперва искать в `src/shared/components`; новые — по skill `ui-components` (+ skill `forms` для форм, `data-layer` для API-слоя)
4. Проверка: `pnpm build` + живой прогон затронутого флоу
5. Обновить этот файл (чекбокс, лог решений) → коммит `feat: этап N — <название>`
