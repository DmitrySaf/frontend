# Bean — мастер-план разработки веб-приложения

> **Назначение:** единый навигационный документ по всей работе. После одобрения копируется в `frontend/docs/plan.md` и обновляется после каждого этапа (чекбоксы, лог решений). Работаем строго по этапам: **вопросы по странице → мок-данные по схеме → реализация → проверка → коммит**.

## Контекст

Строим **Bean** — русскоязычную платформу, где авторы ведут платные сообщества (курсы, посты, чаты) и зарабатывают на подписках. Кодовая база — Next.js 15 (App Router) + Supabase + React Query + Tailwind v4, архитектура FSD (`app → pages → widgets → features → entities → shared`).

**Источники истины:**
- Дизайн-система: `bean-design-system/project/` (README = гайдлайны, `wireframes/*.jsx` = вайрфреймы недостающих экранов, `ui_kits/bean-app/` = готовые референсы экранов, `colors_and_type.css` = токены)
- UX-референсы: Discord (структура сообщества, чат) и Whop (монетизация, витрина)
- Контракт данных: `frontend/docs/db-schema.md` (утверждён; моки повторяют его форму)
- Копирайтинг: русский, sentence case, обращение на «вы», без эмодзи

## Глобальные правила (утверждены пользователем)

| Решение | Значение |
|---|---|
| Бренд | **Bean** (лаймовый боб), ProFound удалён |
| Данные | **Сначала UI на моках** по форме схемы; реальные миграции + подключение БД — предпоследний этап |
| Платежи | **Симуляция**: «оплата» мгновенна, транзакции пишутся в БД; реальный провайдер — после v1 |
| Тарифы | Полная гибкость: recurring с любым периодом/ценой/скидкой, one-time платёж за вход, бесплатно = без активных тарифов |
| Комментарии | Раскрывающаяся лента под постом, один уровень |
| Чат v1 | Отправка + редактирование/удаление своих + модерация админом. Реакции/reply — v2 |
| Видео курсов | Supabase Storage (bucket `lesson-videos`), HTML5-плеер, signed URLs |
| OAuth VK/Яндекс | Скрыт до реальной интеграции |
| OTP | 6 цифр (дефолт Supabase), стилизация по DS |
| Редирект после входа | Последнее сообщество (`localStorage bean:last-community`) → первое → пустое состояние с CTA |
| Rail-кнопки «сообщения»/«поиск» | Скрыты до v2 |
| Аватары без лого | Инициалы на детерминированном пастельном фоне (Discord-style), общий компонент `Avatar` |
| Модалка создания | Без загрузки лого (лого — в админке «Внешний вид») |
| Роли | Модель A: инлайн-переключатель «Просмотр ↔ Редактирование» для админа |
| Адаптив | Desktop-first без жёстких ширин; мобильный UI — после v1 |
| Git | Коммит после каждого завершённого и проверенного этапа (`feat: этап N — …`) |

## Статус этапов

- [x] **Этап 1 — Фундамент**: аудит, ребрендинг, схема БД
- [x] **Этап 2 — Авторизация**
- [x] **Этап 3 — Rail + создание сообщества**
- [x] **Этап 4 — Сайдбар сообщества: категории, табы, «Новый таб»**
- [x] **Этап 5 — Чат**
- [x] **Этап 6 — Посты**
- [x] **Этап 7 — Курсы**
- [x] **Этап 8 — Админ-разделы (дашборд, внешний вид, настройки)**
- [x] **Этап 9 — Публичная витрина + вступление**
- [ ] **Этап 10 — Настройки: верификация, вывод средств, транзакции** ← *текущий*
- [ ] **Этап 11 — Роли (модель A) по всем поверхностям**
- [ ] **Этап 12 — ✋ Чекпоинт: полное ревью и аппрув функционала пользователем** (мок-версия целиком тестируется до начала бэкенда)
- [ ] **Этап 13 — База данных: миграции, RLS, подключение, realtime, Storage**
- [ ] **Этап 14 — Рефакторинг: очистка моков, мёртвого кода, упрощение**
- [ ] **Этап 15 — Проверка безопасности** (по документации из репозитория пользователя — запросить в начале этапа; + RLS-аудит, /security-review)
- [ ] **Этап 16 — Финальный e2e-прогон и полировка**

---

## ✅ Этап 1 — Фундамент (готово)

- Аудит кода и дизайн-системы; инвентаризация экранов
- Ребрендинг: wordmark + лого на логине ([Login.tsx](src/pages/login/ui/Login.tsx)), metadata в [layout.tsx](app/layout.tsx), префикс `bean.com/` в модалке создания
- `font-sans`/`font-mono` привязаны к Geist/Geist Mono в [globals.css](app/globals.css)
- Схема данных утверждена → `docs/db-schema.md` (18 таблиц)

## ✅ Этап 2 — Авторизация (готово)

- OTP-ячейки по DS (Geist Mono, «·», focus-ring 2px primary, danger-состояние) — [ConfirmationForm.tsx](src/features/auth/ui/ConfirmationForm.tsx)
- Кулдаун 60 с на повторную отправку; спиннер + «Отправляем код…» (доработан [button.tsx](src/shared/components/button.tsx): isLoading не заменяет текст)
- OAuth-кнопки удалены
- Редиректы: `/` по сессии; `/communities`-резолвер ([CommunityListPage](src/pages/community-list/ui/CommunityListPage.tsx)): последнее (пишется `CommunityVisitTracker` в layout сообщества) → первое → пустое состояние «Создайте своё сообщество» (CTA открывает модалку через `?create=community`)
- Попутно починено 5 ранее сломанных ошибок сборки (реэкспорты, `Omit`→`Exclude`, типы community API, prerender nuqs → `force-dynamic` на (authenticated)/layout)
- Проверено: build проходит, живой сервер отдаёт корректные редиректы и брендинг

**Осталось вне v1:** реальная интеграция VK ID / Яндекс ID.

## ✅ Этап 3 — Rail + создание сообщества (готово)

- **`Avatar`** ([Avatar.tsx](src/shared/components/Avatar.tsx)): Radix-примитив, `src?`/`name`/`size s|m|l`/`shape square|circle`, инициал на детерминированном пастельном фоне (8 цветов от хэша имени)
- **`Tooltip`** ([Tooltip.tsx](src/shared/components/Tooltip.tsx)): Radix, тёмная плашка по DS (12.5px, rounded-xl, стрелка)
- **Rail**: кнопки «сообщения»/«поиск» скрыты до v2; 🐼 → `Avatar` (square); активный индикатор 3×14px чёрная полоса справа; тултипы названий; лого кликабелен → `/communities`
- **ProfileButton**: реальный профиль (`useProfileQuery`), клик по имени/шестерёнке → `/settings/profile`, «···» → dropdown «Выйти из аккаунта» (danger)
- **Модалка создания**: helper «Уникальное имя — подставляется автоматически», после создания — редирект в новое сообщество
- Установлены `@radix-ui/react-avatar`, `@radix-ui/react-tooltip`

**Осталось вне v1:** мессенджер и глобальный поиск (кнопки rail вернуть при реализации).

## ✅ Этап 4 — Сайдбар сообщества (готово)

**Решения:** «Главной» нет — вход в сообщество открывает первый таб (Discord-style); шапка сообщества (⌘K/help/bell) убрана целиком; «Покинуть» = конферм-модалка (владельцу пункт не показывается); DnD порядка табов — v2.

- **Mock-слой**: [mockCollection.ts](src/shared/utils/mockCollection.ts) — localStorage-коллекции формы db-schema; entity [channel](src/entities/channel/index.ts) (категории+каналы, сид «Начало»+#общий-чат, slug-транслитерация с уникальностью, CRUD-мутации с тостами)
- **CommunitySidebar** переписан: баннер (обложка 132px + оверлей / плоский 60px, Avatar square), chevron-меню (админ: Настройки/Внешний вид/Дашборд + подпись «только для админа»; Пригласить; Покинуть danger), категории со сворачиванием и «+» по hover, строки каналов с иконкой типа (hash/newspaper/book-open) и active-состоянием по DS
- **Модалка «Новый таб»** ([channel-create-modal](src/widgets/channel-create-modal/ui/ChannelCreateModal.tsx)): 3 плитки типов, название с плейсхолдером по типу, селект категории + «Новая категория» инлайн; после создания — переход в таб
- **Инвайт**: диалог с копируемой ссылкой на публичную страницу
- **Роутинг**: `[slug]` → редирект в первый таб; `[tabSlug]` → тайтл-бар (имя+тип-пилл) + плейсхолдер по типу (заменяются на этапах 5–7); `[slug]/admin/{dashboard,appearance,settings}` — заглушки «в разработке» (этап 8)
- **Dropdown** расширен: note-подписи («только для админа»), danger-токен вместо red
- Удалены: `community-header`, `community-home`, старые статичные MAIN_TABS/SIDEBAR_SECTIONS

## ✅ Этап 5 — Чат (готово)

**Решения:** без вложений и эмодзи-пикера в v1 (кнопки «+»/смайл убраны из композера); лимит 2000 символов (счётчик появляется после 1800); при первом открытии чата сидируется демо-история от вымышленных участников (вчера+сегодня — видны разделители и группировка).

- Entity [message](src/entities/message/index.ts): mock-store формы `messages` (soft delete), сид-история, CURRENT_USER_ID=«me» до подключения auth, MOCK_MEMBERS
- [ChatScreen](src/pages/community-channel/ui/chat/ChatScreen.tsx): разделители дней («сегодня»/«вчера»/дата), группировка сообщений одного автора в окне 5 минут, метка «автор» (primary) у владельца, автоскролл к низу
- Сообщение: hover-меню «···» — редактировать (инлайн-textarea, Enter/Esc, метка «(изменено)») и удалить (конферм-диалог); админ может удалять чужие
- [Композер](src/pages/community-channel/ui/chat/ChatComposer.tsx): авторастущий textarea (до 5 строк), Enter — отправить, Shift+Enter — перенос, при ошибке текст возвращается в поле
- Тосты только на ошибки (успех в чате виден мгновенно — как в Discord)

**Осталось вне v1:** вложения, реакции, ответы, typing-индикатор, пагинация вверх (история пока маленькая).

## ✅ Этап 6 — Посты (готово)

**Решения:** редактор — инлайн-расширение композера на месте (LinkedIn-style); фильтр-пиллы и «Поделиться» убраны из v1 (темы решаются отдельными табами, публичных ссылок на посты пока нет); в новом табе сидируются 2 демо-поста (закреплённый с обложкой от владельца + текстовый от участницы с лайками и комментариями).

- Entity [post](src/entities/post/index.ts) переписана: mock-store форм `posts`/`post_likes`/`post_bookmarks`/`post_comments`, агрегация счётчиков и флагов likedByMe/bookmarkedByMe, сортировка «закреплённые выше»
- [PostsScreen](src/pages/community-channel/ui/posts/PostsScreen.tsx): лента 720px, пустое состояние, удаление с конфермом
- [PostCard](src/pages/community-channel/ui/posts/PostCard.tsx): автор-строка («· автор» primary, отн. время, лаймовый пилл «Закреплено»), заголовок, текст, обложка full-bleed, реакции (heart→danger, комментарии-счётчик, bookmark); «···»-меню: свой — редактировать/удалить, админ — закрепить/открепить/удалить любой
- [PostForm](src/pages/community-channel/ui/posts/PostForm.tsx): заголовок+текст (RHF+zod), обложка через `fileToDataUrl` (canvas-даунскейл до 1280px, jpeg — переживает перезагрузку в localStorage); редактирование поста — та же форма внутри карточки
- [Комментарии](src/pages/community-channel/ui/posts/PostComments.tsx): раскрывающаяся лента + поле ввода (Enter), один уровень
- Общий [useAuthorView](src/pages/community-channel/ui/useAuthorView.ts) для чата и постов
- Утилиты: `formatRelativeTime` («3 часа назад», «вчера, 18:40»), `fileToDataUrl`

**Осталось вне v1:** фильтры/типы постов, шаринг, страница отдельного поста.

## ✅ Этап 7 — Курсы (готово)

**Решения:** последовательное прохождение (урок открыт после отметки предыдущего пройденным); видео опционально (текстовые уроки поддерживаются); в мок-режиме все видео играют общий демо-ролик (BigBuckBunny), а «загрузка» пишет фиктивный `video_path` + длительность из метаданных файла; новый таб-курс создаётся пустым (без сида).

- Entity [course](src/entities/course/index.ts): mock-store форм `courses`/`course_modules`/`course_lessons`/`lesson_progress`; курс создаётся 1:1 с табом при первом открытии; маппер считает сквозную нумерацию, прогресс и последовательную разблокировку
- **Плеер** ([LessonView](src/pages/community-channel/ui/course/LessonView.tsx)): видео/текстовый блок, пиллы «Урок N из M» + длительность, Назад / Следующий урок (недоступен, пока текущий не пройден) / «Отметить пройденным» (toggle)
- **Список уроков** ([LessonList](src/pages/community-channel/ui/course/LessonList.tsx)): прогресс-бар N/M, модули «Модуль N · название», строки с check/play/lock и таймингом Geist Mono; заблокированные некликабельны
- **Конструктор (модель A)** — [SegmentedControl](src/shared/components/SegmentedControl.tsx) «Просмотр ↔ Редактирование» (новый компонент UI-кита по DS): инлайн-переименование/удаление модулей, «+ Добавить модуль/урок» (урок создаётся сразу и открывается в редакторе), [LessonEditor](src/pages/community-channel/ui/course/LessonEditor.tsx) — название/описание (RHF+zod), загрузка видео с чтением длительности (`getVideoFileDuration`), удаление с конфермом
- Утилиты: `formatDuration` (мм:сс), `getVideoFileDuration`

**Осталось вне v1:** перестановка модулей/уроков (DnD), материалы-файлы к уроку, реальная загрузка в Storage.

## ✅ Этап 8 — Админ-разделы (готово)

**Решения:** дашборд наполняется сид-историей продаж за 12 месяцев (подписки+транзакции формы схемы; при отсутствии тарифов сидируются 3 демо-тарифа); danger-зона «Удалить сообщество» в настройках; акцентный цвет и тема убраны из v1; обложка/лого применяются сразу везде (баннер сайдбара, rail, live-превью).

- **Community profile**: mock extras (описание/обложка/лого/видимость) поверх реальной таблицы + `updateCommunity` (display_name) в Supabase-слое; `useCommunityProfileQuery`, `useCommunityLogosQuery` (rail)
- Entities [tier](src/entities/tier/index.ts) (CRUD, тумблер is_active, formatTierPrice) и [subscription](src/entities/subscription/index.ts) (сид продаж, purchaseTier для этапа 9, transformCommunityStats)
- **Дашборд** ([DashboardPage](src/pages/community-admin/ui/dashboard/DashboardPage.tsx)): 3 стат-карты (Geist Mono, тренды с дельтами), бар-чарт дохода 12 мес (текущий месяц primary, ховер-тултипы по dataviz), лайн-чарт роста участников (2px, ховер-точки), таблица тарифов
- **Внешний вид** ([AppearancePage](src/pages/community-admin/ui/appearance/AppearancePage.tsx)): загрузка обложки/лого (canvas-даунскейл), live-превью публичной карточки, Сохранить активен только при изменениях
- **Настройки** ([CommunitySettingsPage](src/pages/community-admin/ui/settings/CommunitySettingsPage.tsx)): название+описание (RHF+zod), slug read-only («станет доступно позже»), видимость-карточки, тарифы CRUD ([TierFormModal](src/pages/community-admin/ui/settings/TierFormModal.tsx): подписка/разовый, цена/период/скидка) + danger-зона удаления с конфермом
- [AdminShell](src/pages/community-admin/ui/AdminShell.tsx) — общий каркас с «назад» и «только для админа»

**Осталось вне v1:** смена публичного адреса, периоды дашборда, акцент/тема сообщества.

## ✅ Этап 9 — Публичная витрина + вступление (готово)

**Спецификация приватности утверждена 2026-07-04** — см. `docs/db-schema.md`: visibility hidden(default)/unlisted/live; прямой URL ≠ доступ; hidden → единообразный 404; инвайты с lifecycle; каналы open/private/secret (реализация доступа каналов — этап 11); витрина — самостоятельная сущность.

Сделано:
- Visibility hidden/unlisted/live (3 карточки в настройках, default hidden, миграция старых значений в геттере)
- Entities: [member](src/entities/member/index.ts) (membership, join/leave), [invite](src/entities/invite/index.ts) (криптослучайные коды, validate/consume/revoke, uses не тратится действующим участником), [storefront](src/entities/storefront/index.ts) (описание, медиа, пункты «Что внутри» с фикс-набором иконок)
- Редактор витрины во «Внешнем виде» ([StorefrontEditor](src/pages/community-admin/ui/appearance/StorefrontEditor.tsx))
- [StorefrontPage](src/pages/community-preview/ui/StorefrontPage.tsx) `/[slug]?invite=CODE`: единый 404-экран для hidden/несуществующих без валидного инвайта; карусель медиа; тарифная карточка (активные, скрытые тарифы видны только по инвайту; free join без тарифов); «уже участник» → «Открыть сообщество»; карточка автора
- Вступление: гость → [OTP-диалог](src/pages/community-preview/ui/AuthRequiredDialog.tsx) (фича auth c onSuccess) → продолжение действия; платно → [модалка-чек](src/pages/community-preview/ui/CheckoutModal.tsx) с плашкой тестового режима → purchaseTier + membership + трата инвайта → редирект внутрь
- Инвайт-диалог сайдбара: реальный код, счётчик использований, отзыв с автосозданием новой ссылки

**Открытые проверки к этапам 12–13:** anon-RLS select на communities для гостевой витрины (Supabase-проект был недоступен при проверке); серверный 404 для hidden (сейчас проверка на клиенте из-за localStorage-моков).

## Этап 10 — Настройки: верификация, вывод средств, транзакции

**Референсы:** `wire-settings.jsx` (все 5 экранов + пустые состояния), `wire-app.jsx` секция settings.

Задачи:
1. **Верификация**: баннер 3 состояний (none: recess + CTA «Пройти верификацию»; pending: янтарный «до 2 рабочих дней»; passed: зелёный + значок в профиле) — семантические токены из DS (`--warning`/`--success` добавить в `@theme`); флоу подачи: выбор типа (паспорт/самозанятый/ИП/ООО) + форма (mock) → pending
2. **Вывод средств**: список карт (радио «Выбрана для вывода», номер Geist Mono, «···»-меню удаления), модалка «Добавить карту» (ФИО раздельно + номер), пустое состояние
3. **Транзакции**: фильтры Все/Поступления/Выводы, строки (иконка направления, название, дата, статус-пилл, ±сумма Geist Mono), пустое состояние; кнопка «Вывести средства» (симуляция: списание → транзакция «В обработке»)
4. **Профиль/Конфиденциальность**: сверить с вайрфреймом (sticky save-bar, EditRow «Изменить» для почты/телефона, тумблеры приватности) — уже реализованы, полировка

**Вопросы перед этапом:** смена почты — с подтверждением кодом; телефон в v1 (в БД поля нет — добавить в схему?); баланс — где показывать (шапка транзакций?).

## Этап 11 — Роли (модель A) + доступ каналов

**Референсы:** `wire-roles-models.jsx` (матрица прав), `wire-roles-posts.jsx`, `wire-roles-courses.jsx`, `roles-app.jsx`, спецификация приватности в db-schema.

Задачи:
1. Hook `useCommunityRole(slug)` поверх membership (этап 9) + owner-детекция
2. Глобальный переключатель «Просмотр ↔ Редактирование» для админа (курсы уже; посты-модерация, сайдбар «+»)
3. Скрытие админ-пунктов (chevron-меню, admin-маршруты — guard), «···»-меню по ролям, модерация чата, композер постов только админам
4. **Доступ каналов**: `open`/`private`/`secret` в модалке «Новый таб» и настройках канала; замки в сайдбаре; скрытие secret; `channel_grants`; invite-ссылка на secret-канал = membership+грант одним действием; неретроактивность
5. **Управление инвайтами**: список активных ссылок сообщества, создание любым участником, отзыв любым админом
6. Матрица прав задокументировать в `docs/db-schema.md` (для RLS)

## Этап 12 — ✋ Чекпоинт: ревью функционала

Пользователь целиком прогоняет мок-версию приложения (оба сценария: автор и участник) и даёт аппрув или список правок. Бэкенд не начинаем без аппрува. Правки по итогам ревью — здесь же.

## Этап 13 — База данных: миграции и подключение

Задачи:
1. Миграции Supabase по `docs/db-schema.md` (все 15 новых таблиц + расширение communities) + RLS-политики по матрице прав + `pnpm gen-supabase-types`
2. Storage-бакеты: `community-covers`, `community-logos`, `post-covers`, `lesson-videos` (+политики)
3. Замена mock-store на реальные API послойно (entity за entity: members → channels → messages → posts → courses → tiers/subscriptions/transactions → verification/payout), сохраняя формы типов
4. Realtime: чат (insert/update/delete messages), посты, инвалидация кэша по skill `realtime`
5. Симуляция платежей: RPC/edge function `simulate_purchase` → subscription + transaction атомарно
6. Сиды для демо-данных

## Этап 14 — Рефакторинг

1. Удаление mock-слоя (mockCollection, сиды, CURRENT_USER_ID, MOCK_MEMBERS) после подключения БД
2. Очистка мёртвого кода/экспортов, унификация повторяющихся паттернов (карточки, пустые состояния)
3. Прогон /simplify + /code-review, `pnpm check` чистый

## Этап 15 — Проверка безопасности

1. **В начале этапа запросить у пользователя репозиторий с документацией по безопасности** (он прикрепит его к этому моменту)
2. Аудит RLS-политик всех таблиц (матрица прав из этапа 11), Storage-политик
3. Требования спецификации приватности (db-schema): атомарная проверка инвайтов, rate limiting инвайт-эндпоинта, хэширование IP, единообразный 404 для hidden, одноразовые ссылки для secret-грантов
4. Проверка: server-side валидация мутаций, санитизация контента (посты/чат), rate limiting симуляции платежей, утечки данных в публичных запросах
4. Прогон /security-review

## Этап 16 — Финальный e2e-прогон и полировка

1. Сценарий автора: регистрация → создание сообщества → структура (чат/посты/курс) → контент → тарифы → внешний вид → верификация → карта → «продажа» → дашборд → вывод средств → транзакции
2. Сценарий участника: витрина → вступление (бесплатно и платно) → чат/посты/курс → прогресс → покинуть сообщество
3. Все пустые/загрузочные/ошибочные состояния; тосты по паттерну mutations; консистентность DS (радиусы, цвета, копирайтинг)
4. `pnpm build` + `pnpm check` чистые; финальный коммит

---

## Регламент работы по этапу

1. Перед этапом: перечитать соответствующий вайрфрейм + задать вопросы из секции этапа (AskUserQuestion)
2. Мок-данные строго по `docs/db-schema.md` (snake_case «в БД», camelCase после мапперов)
3. Компоненты: сперва искать в `src/shared/components`; новые — по skill `ui-components` (+ skill `forms` для форм, `data-layer` для API-слоя)
4. Проверка: `pnpm build` + живой прогон затронутого флоу
5. Обновить этот файл (чекбокс, лог решений) → коммит `feat: этап N — <название>`
